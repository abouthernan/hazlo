<section class="calendar-section">
  <div class="calendar-container">

    <!-- Header -->
    <div class="calendar-header">
      <div class="section-badge">
        <span class="badge-icon">📅</span>
        <span class="badge-text">Eventos</span>
      </div>
      <h2 class="calendar-title">Hazlo en <span class="gradient-text">Vivo</span></h2>
      <p class="calendar-description">
        Todas las semanas tenemos sesiones virtuales en vivo para que aprendas haciendo. Únete, pregunta, practica y construye junto a la comunidad.
      </p>
    </div>

    <!-- Luma Calendar Embed -->
    <div class="calendar-embed">
      <iframe
        src="https://luma.com/embed/calendar/cal-JCUlotEDabveY2g/events"
        width="100%"
        height="500"
        frameborder="0"
        style="border: 1px solid #bfcbda88; border-radius: 12px;"
        allowfullscreen=""
        aria-hidden="false"
        tabindex="0"
      ></iframe>
    </div>

  </div>
</section>

<style>
  .calendar-section {
    padding: var(--space-3xl) 0;
  }

  .calendar-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 var(--space-md);
    display: flex;
    flex-direction: column;
    gap: var(--space-2xl);
  }

  /* Header */
  .calendar-header {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: var(--space-sm);
  }

  .section-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.35rem 0.9rem;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 999px;
    font-size: 0.8rem;
    font-weight: 500;
    color: var(--text-secondary);
    margin-bottom: var(--space-xs);
  }

  .badge-icon {
    font-size: 0.9rem;
  }

  .calendar-title {
    font: var(--font-h2);
    color: var(--text-primary);
    letter-spacing: -0.025em;
    margin: 0;
  }

  .gradient-text {
    background: linear-gradient(135deg, var(--primary-purple) 0%, var(--primary-blue) 100%);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .calendar-description {
    font: var(--font-body-lg);
    color: var(--text-secondary);
    max-width: 540px;
    margin: 0;
    line-height: 1.7;
  }

  /* Embed */
  .calendar-embed {
    width: 100%;
    border-radius: 12px;
    overflow: hidden;
  }

  .calendar-embed iframe {
    display: block;
    width: 100%;
    min-height: 500px;
  }

  /* Mobile */
  @media (max-width: 768px) {
    .calendar-embed iframe {
      min-height: 420px;
    }
  }

  @media (max-width: 480px) {
    .calendar-embed iframe {
      min-height: 360px;
    }
  }
</style>
