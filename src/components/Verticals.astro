<section class="verticals-section">
  <div class="verticals-container">

    <div class="section-header">
      <div class="section-badge">
        <span class="badge-icon">⚡</span>
        <span class="badge-text">Lo que hacemos</span>
      </div>
      <h2 class="section-title">
        Así transformamos <span class="title-gradient">la región</span>
      </h2>
    </div>

    <div class="cards-grid">

      <!-- Comunidad -->
      <a href="#" class="vertical-card card-comunidad">
        <img src="/images/comunidad.jpg" alt="Comunidad" class="card-bg" />
        <div class="card-overlay"></div>
        <div class="card-content">
          <span class="card-arrow">→</span>
          <h3 class="card-title">Comunidad</h3>
          <p class="card-subtitle">Emprendimiento / IA / Startups</p>
        </div>
      </a>

      <!-- Formación -->
      <a href="#" class="vertical-card card-formacion">
        <img src="/images/formacion.jpg" alt="Formación" class="card-bg" />
        <div class="card-overlay"></div>
        <div class="card-content">
          <span class="card-arrow">→</span>
          <h3 class="card-title">Formación</h3>
          <p class="card-subtitle">IA / Vibecoding / Growth</p>
        </div>
      </a>

      <!-- Empresas -->
      <a href="https://wa.me/573133846633" target="_blank" rel="noopener noreferrer" class="vertical-card card-empresas">
        <img src="/images/empresas.jpg" alt="Empresas" class="card-bg" />
        <div class="card-overlay"></div>
        <div class="card-content">
          <span class="card-arrow">→</span>
          <h3 class="card-title">Empresas</h3>
          <p class="card-subtitle">Capacitación / Adopción IA / Growth</p>
        </div>
      </a>

    </div>
  </div>
</section>

<style>
  .verticals-section {
    padding: var(--space-3xl) 0;
  }

  .verticals-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 var(--space-md);
  }

  .section-header {
    text-align: center;
    margin-bottom: var(--space-2xl);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-sm);
  }

  .section-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1.5rem;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.15);
    border-radius: 50px;
    font-size: var(--font-small);
    color: var(--text-secondary);
    backdrop-filter: blur(10px);
  }

  .section-title {
    font: var(--font-h2);
    font-weight: 800;
    color: var(--text-primary);
    margin: 0;
  }

  .title-gradient {
    background: linear-gradient(135deg, var(--primary-purple) 0%, var(--primary-blue) 100%);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  /* Grid */
  .cards-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-md);
  }

  /* Imagen de fondo */
  .card-bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 0;
  }

  /* Card base */
  .vertical-card {
    position: relative;
    height: 480px;
    border-radius: var(--radius-xl);
    overflow: hidden;
    text-decoration: none;
    cursor: pointer;
    transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1), box-shadow 0.4s ease;
  }

  .vertical-card:hover {
    transform: translateY(-6px);
    box-shadow: 0 24px 48px -12px rgba(0, 0, 0, 0.4);
  }

  .vertical-card:hover .card-arrow {
    transform: translateX(6px);
  }

  /* Fondos por tarjeta */
  .card-comunidad {
    background:
      linear-gradient(160deg, #3b2d6e 0%, #1a1a2e 60%, #0d1117 100%);
  }

  .card-comunidad::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse at 30% 40%, rgba(177, 152, 233, 0.35) 0%, transparent 65%),
      radial-gradient(ellipse at 80% 80%, rgba(107, 232, 182, 0.15) 0%, transparent 50%);
  }

  .card-formacion {
    background:
      linear-gradient(160deg, #1a2a3e 0%, #0f1f2e 60%, #0d1117 100%);
  }

  .card-formacion::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse at 50% 35%, rgba(107, 232, 182, 0.3) 0%, transparent 65%),
      radial-gradient(ellipse at 20% 80%, rgba(177, 152, 233, 0.15) 0%, transparent 50%);
  }

  .card-empresas {
    background:
      linear-gradient(160deg, #1e2d1e 0%, #0f1f12 60%, #0d1117 100%);
  }

  .card-empresas::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse at 70% 30%, rgba(107, 232, 182, 0.35) 0%, transparent 60%),
      radial-gradient(ellipse at 30% 75%, rgba(177, 152, 233, 0.12) 0%, transparent 55%);
  }

  /* Overlay gradiente inferior para legibilidad */
  .card-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to bottom,
      transparent 35%,
      rgba(0, 0, 0, 0.3) 65%,
      rgba(0, 0, 0, 0.75) 100%
    );
    z-index: 1;
  }

  /* Contenido de la tarjeta */
  .card-content {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: var(--space-lg) var(--space-lg) var(--space-xl);
    z-index: 2;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  .card-arrow {
    font-size: 1.25rem;
    color: rgba(255, 255, 255, 0.7);
    display: block;
    transition: transform 0.3s ease;
    margin-bottom: 0.25rem;
  }

  .card-title {
    font-size: 2.25rem;
    font-weight: 800;
    color: #ffffff;
    letter-spacing: -0.02em;
    line-height: 1.1;
    margin: 0;
  }

  .card-subtitle {
    font-size: 0.95rem;
    color: rgba(255, 255, 255, 0.65);
    margin: 0;
    line-height: 1.5;
  }

  /* Responsive */
  @media (max-width: 900px) {
    .cards-grid {
      grid-template-columns: 1fr;
      gap: var(--space-sm);
    }

    .vertical-card {
      height: 320px;
    }

    .card-title {
      font-size: 1.75rem;
    }
  }
</style>
