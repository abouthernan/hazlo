---
// src/components/Hero.astro
---

<section class="hero">
  <div class="hero-container">
    <div class="hero-content">

         <!-- Badge/Pill superior -->
      <div class="hero-badge mobile-mode">
        <span class="badge-icon">✨</span>
        <span class="badge-text">La Comunidad</span>
      </div>
     

      <!-- Título principal -->
        <h1 class="hero-title">
            <span id="dynamicText">¿Quieres aprender sobre IA?</span>
            <span class="title-gradient">hagámoslo juntos.</span>
        </h1>

      <!-- Subtítulo -->
      <p class="hero-subtitle">
        Somo una comunidad de emprededores, de personas apasionadas por la tecnología, la IA y el Vibecoding. Nos encanta construir y crear valor para el ecosistema de emprendimiento de LATAM.
      </p>

      <!-- CTAs -->
      <div class="hero-actions">
        <a href="https://chat.whatsapp.com/KDSfG6NQcyK0hLDo2NpKbh?mode=ems_copy_t" class="btn btn-primary hero-cta-primary">
          <span>Unierme ahora</span>
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </a>
        </a>
      </div>

      

      <!-- Social proof / Stats -->
      <div class="hero-stats">
        <div class="stat-item">
          <span class="stat-number">+2.5K</span>
          <span class="stat-label">Emprendedores activos</span>
        </div>
        <div class="stat-divider"></div>
        <div class="stat-item">
          <span class="stat-number">+200</span>
          <span class="stat-label">Proyectos Publicados</span>
        </div>
        <div class="stat-divider"></div>
        <div class="stat-item">
          <span class="stat-number">95%</span>
          <span class="stat-label">Ideas Validadas</span>
        </div>
      </div>
    </div>


  </div>
</section>

<!-- Video Modal
<div id="videoModal" class="video-modal">
  <div class="modal-backdrop" onclick="closeVideoModal()"></div>
  <div class="modal-content">
    <button class="modal-close" onclick="closeVideoModal()" aria-label="Cerrar video">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <line x1="18" y1="6" x2="6" y2="18"></line>
        <line x1="6" y1="6" x2="18" y2="18"></line>
      </svg>
    </button>
    <div class="video-container">
      <iframe id="demoVideo" 
        src="" 
        frameborder="0" 
        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" 
        allowfullscreen>
      </iframe>
    </div>
  </div>
</div> -->

<style>

  #dynamicText {
    transition: opacity 0.3s ease;
 }

  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding-top: 10rem;
    /**padding: 6rem 0 4rem;**/
    position: relative;
  }

  .hero-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 var(--space-md);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-3xl);
    align-items: center;
  }

  .hero-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
  }

  /* Badge */
  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: var(--space-xs);
    padding: var(--space-xs) var(--space-md);
    background: rgba(255, 255, 255, 0.1);
    border: 0px solid rgba(255, 255, 255, 0.2);
    border-radius: 50px;
    font-size: var(--font-small);
    color: var(--text-secondary);
    backdrop-filter: blur(10px);
    width: fit-content;
    transition: var(--transition);
  }

  .hero-badge:hover {
    background: rgba(255, 255, 255, 0.15);
    transform: translateY(-2px);
  }

  .badge-icon {
    font-size: 1rem;
  }

  /* Título */
  .hero-title {
    font: var(--font-h1);
    color: var(--text-primary);
    line-height: 1.1;
    margin: 0;
  }

  .title-gradient {
    background: linear-gradient(135deg, var(--primary-purple) 0%, var(--primary-blue) 100%);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    position: relative;
  }

  /* Subtítulo */
  .hero-subtitle {
    font: var(--font-body-lg);
    color: var(--text-secondary);
    max-width: 90%;
    line-height: 1.6;
  }

  /* Actions */
  .hero-actions {
    display: flex;
    gap: var(--space-md);
    margin-top: var(--space-sm);
  }

  .hero-cta-primary {
    display: flex;
    align-items: center;
    gap: var(--space-xs);
    padding: var(--space-md) var(--space-xl);
    font-size: 1rem;
    font-weight: 600;
  }

  .hero-cta-secondary {
    display: flex;
    align-items: center;
    gap: var(--space-xs);
    padding: var(--space-md) var(--space-xl);
    font-size: 1rem;
    font-weight: 600;
  }

  /* Stats */
  .hero-stats {
    display: flex;
    align-items: center;
    gap: var(--space-lg);
    margin-top: var(--space-lg);
    padding-top: var(--space-lg);
    border-top: 1px solid var(--border-light);
  }

  .stat-item {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .stat-number {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--text-primary);
  }

  .stat-label {
    font-size: var(--font-small);
    color: var(--text-muted);
  }

  .stat-divider {
    width: 1px;
    height: 2rem;
    background: var(--border-light);
  }

  /* Visual Elements */
  

  /* Chat Animation Styles */
  .hero-visual {
    position: relative;
    height: 500px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .chat-container {
    width: 350px;
    height: 450px;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 20px;
    backdrop-filter: blur(20px);
    overflow: hidden;
    box-shadow: var(--shadow-lg);
    display: flex;
    flex-direction: column;
    animation: chatFloat 6s ease-in-out infinite;
  }

  @keyframes chatFloat {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
  }

  /* Chat Header */
  .chat-header {
    display: flex;
    align-items: center;
    padding: var(--space-md);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    background: rgba(255, 255, 255, 0.05);
  }

  .chat-avatar {
    margin-right: var(--space-sm);
  }

  .avatar-gradient {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--primary-purple) 0%, var(--primary-blue) 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-weight: 700;
    font-size: 1.2rem;
  }

  .chat-info {
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  .chat-name {
    font-weight: 600;
    color: var(--text-primary);
    font-size: 1rem;
  }

  .chat-status {
    font-size: 0.8rem;
    color: #10B981;
    margin-top: 2px;
  }

  .status-dot {
    width: 8px;
    height: 8px;
    background: #10B981;
    border-radius: 50%;
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }

  /* Chat Messages */
  .chat-messages {
    flex: 1;
    padding: var(--space-md);
    overflow-y: auto;
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .chat-messages::-webkit-scrollbar {
    width: 4px;
  }

  .chat-messages::-webkit-scrollbar-thumb {
    background: rgba(255, 255, 255, 0.2);
    border-radius: 2px;
  }

  .message {
    display: flex;
    align-items: flex-end;
    gap: var(--space-xs);
    opacity: 0;
    transform: translateY(20px);
    animation: messageAppear 0.5s ease-out forwards;
  }

  @keyframes messageAppear {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .message.user {
    flex-direction: row-reverse;
    align-self: flex-end;
  }

  .message.hazlo {
    flex-direction: row;
    align-self: flex-start;
  }

  .message-avatar {
    width: 24px;
    height: 24px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--primary-purple) 0%, var(--primary-blue) 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-weight: 600;
    font-size: 0.7rem;
    flex-shrink: 0;
  }

  .user .message-avatar {
    background: #3B82F6;
  }

  .message-bubble {
    max-width: 70%;
    padding: var(--space-sm) var(--space-md);
    border-radius: 18px;
    font-size: 0.9rem;
    line-height: 1.4;
    position: relative;
  }

  .message.user .message-bubble {
    background: #3B82F6;
    color: white;
    border-bottom-right-radius: 4px;
  }

  .message.hazlo .message-bubble {
    background: rgba(255, 255, 255, 0.15);
    color: var(--text-primary);
    border-bottom-left-radius: 4px;
  }

  .message-time {
    font-size: 0.7rem;
    color: var(--text-muted);
    margin-top: 4px;
    text-align: right;
  }

  .message.hazlo .message-time {
    text-align: left;
  }

  /* Typing Indicator */
  .typing-indicator {
    display: flex;
    align-items: center;
    gap: var(--space-xs);
    padding: 0 var(--space-md) var(--space-md);
    opacity: 0;
    transform: translateY(10px);
  }

  .typing-indicator.show {
    opacity: 1;
    transform: translateY(0);
    animation: messageAppear 0.3s ease-out;
  }

  .typing-indicator .avatar-gradient {
    width: 24px;
    height: 24px;
    font-size: 0.7rem;
  }

  .typing-bubble {
    background: rgba(255, 255, 255, 0.15);
    border-radius: 18px;
    padding: var(--space-sm) var(--space-md);
    border-bottom-left-radius: 4px;
  }

  .typing-dots {
    display: flex;
    gap: 4px;
  }

  .typing-dots span {
    width: 6px;
    height: 6px;
    background: var(--text-muted);
    border-radius: 50%;
    animation: typingDot 1.4s infinite;
  }

  .typing-dots span:nth-child(2) {
    animation-delay: 0.2s;
  }

  .typing-dots span:nth-child(3) {
    animation-delay: 0.4s;
  }

  @keyframes typingDot {
    0%, 60%, 100% { opacity: 0.3; }
    30% { opacity: 1; }
  }





  .visual-card {
    position: absolute;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: var(--radius-lg);
    backdrop-filter: blur(20px);
    padding: var(--space-lg);
    transition: var(--transition);
  }

  .visual-card:hover {
    transform: translateY(-5px);
    background: rgba(255, 255, 255, 0.15);
  }

  .card-1 {
    width: 280px;
    top: 50px;
    left: 0;
    animation: float 6s ease-in-out infinite;
  }

  .card-2 {
    width: 250px;
    bottom: 80px;
    right: 20px;
    animation: float 6s ease-in-out infinite reverse;
  }

  .card-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: var(--space-md);
    padding-bottom: var(--space-sm);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .card-dots {
    display: flex;
    gap: 6px;
  }

  .dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
  }

  .dot-red { background: #ff5f57; }
  .dot-yellow { background: #ffbd2e; }
  .dot-green { background: #28ca42; }

  .card-title {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--text-primary);
  }

  .card-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .card-stat {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .stat-bar {
    height: 8px;
    border-radius: 4px;
    background: linear-gradient(135deg, var(--primary-purple) 0%, var(--primary-blue) 100%);
    opacity: 0.8;
  }

  .bar-1 { width: 85%; }
  .bar-2 { width: 65%; }
  .bar-3 { width: 90%; }

  .stat-text {
    font-size: 0.75rem;
    color: var(--text-secondary);
  }

  .notification-item {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    padding: var(--space-sm) 0;
  }

  .notification-item:not(:last-child) {
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    margin-bottom: var(--space-sm);
  }

  .notif-icon {
    width: 2rem;
    height: 2rem;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.1);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.875rem;
  }

  .notif-content {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .notif-title {
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--text-primary);
  }

  .notif-time {
    font-size: 0.75rem;
    color: var(--text-muted);
  }

  /* Animations */
  @keyframes float {
    0%, 100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .hero-container {
      grid-template-columns: 1fr;
      gap: var(--space-2xl);
      text-align: center;
    }

    .hero-visual {
      height: 400px;
      /* NO cambiar el order - mantener las imágenes al final */
    }

    .card-1 {
      top: 20px;
      left: 20px;
    }

    .card-2 {
      bottom: 20px;
      right: 20px;
    }

    .chat-container {
      width: 320px;
      height: 400px;
    }






  }

  @media (max-width: 768px) {
    .hero {
      padding: 5rem 0 3rem;
      min-height: auto;
    }

    .hero-container {
      
      padding: 0 var(--space-sm);
      gap: var(--space-xl);
    }

    .hero-title {
      font-size: 2.5rem;
    }

    .hero-subtitle {
      max-width: 100%;
    }

    .hero-actions {
      flex-direction: column;
      align-items: center;
    }

    .hero-cta-primary,
    .hero-cta-secondary {
      width: 100%;
      justify-content: center;
    }

    .hero-stats {
      flex-direction: column;
      gap: var(--space-md);
      text-align: center;
    }

    .stat-divider {
      display: none;
    }

    .hero-visual {
      height: 350px;
    }

    .chat-container {
      width: 300px;
      height: 350px;
    }

    .hero-badge {
      align-self: center;
    }
  }

  /* Video Modal Styles */
  .video-modal {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 9999;
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s ease;
  }

  .video-modal.active {
    opacity: 1;
    visibility: visible;
  }

  .modal-backdrop {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.8);
    backdrop-filter: blur(4px);
  }

  .modal-content {
    position: relative;
    width: 90%;
    max-width: 900px;
    aspect-ratio: 16/9;
    background: var(--surface);
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: var(--shadow-lg);
    transform: scale(0.8);
    transition: transform 0.3s ease;
  }

  .video-modal.active .modal-content {
    transform: scale(1);
  }

  .modal-close {
    position: absolute;
    top: -50px;
    right: 0;
    background: rgba(255, 255, 255, 0.1);
    border: none;
    color: white;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: var(--transition);
    backdrop-filter: blur(10px);
  }

  .modal-close:hover {
    background: rgba(255, 255, 255, 0.2);
    transform: scale(1.1);
  }

  .video-container {
    position: relative;
    width: 100%;
    height: 100%;
  }

  .video-container iframe {
    width: 100%;
    height: 100%;
    border: none;
  }


  @media (max-width: 480px) {
    .hero-title {
      font-size: 3rem;
    }

    .hero-container {
      padding-top: 5rem;
    }

    .mobile-mode {
    display: none;
    }

    .hero-badge {   
        margin-top: 3rem;
      padding: 0.1rem 0.9rem;
      font-size: 0.75rem;
    }

     .chat-container {
      width: 280px;
      height: 320px;
    }

    .message-bubble {
      max-width: 85%;
      font-size: 0.85rem;
    }

    .chat-header .chat-name{
     text-align: left;
    }

    .chat-status {
      text-align: left;
    }

    .message-bubble {
     text-align: left;
    }

    .chat-messages {
      text-align: left;
    }

    .visual-card {
      width: 100% !important;
      max-width: 280px;
    }
    .modal-content {
      width: 98%;
      aspect-ratio: 16/10;
    }

    .modal-close {
      top: 10px;
      right: 10px;
      background: rgba(0, 0, 0, 0.7);
      width: 32px;
      height: 32px;
    }
  }
</style>
